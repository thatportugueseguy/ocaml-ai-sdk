(* MLX requires a structure item before [@react.component] *)
let _mlx = ()

[@react.component]
let make () =
  Chat_layout.render ~title:"Basic Chat"
    ~description:"Simple streaming chat with the useChat hook."
    ~endpoint:"/api/chat/basic" ()
