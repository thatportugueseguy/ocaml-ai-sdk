let () =
  match ReactDOM.querySelector "#root" with
  | Some root ->
    let app = React.createElement Melange_chat.App.make () in
    ReactDOM.Client.render (ReactDOM.Client.createRoot root) app
  | None ->
    Js.Console.error "Could not find #root element"
