(** OCaml AI SDK — Provider abstraction layer.

    Type-safe, provider-agnostic types and module signatures for AI model
    integration. Inspired by Vercel AI SDK's V3 specification. *)

module Provider_options = Provider_options
module Finish_reason = Finish_reason
module Usage = Usage
module Warning = Warning
module Provider_error = Provider_error
module Prompt = Prompt
